import { ProgramDirective } from './program.directive';

describe('ProgramDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgramDirective();
    expect(directive).toBeTruthy();
  });
});
