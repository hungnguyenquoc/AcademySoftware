import { ProgramStudyDirective } from './program-study.directive';

describe('ProgramStudyDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgramStudyDirective();
    expect(directive).toBeTruthy();
  });
});
